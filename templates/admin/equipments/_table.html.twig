{% set columns = [
    { label: 'Équipement', sort: 'label' },
    { label: 'Type d’équipement' },
    { label: 'Actions' }
] %}

<div class="relative overflow-visible rounded-base border border-default bg-neutral-primary-soft shadow-xs">
    <div class="overflow-x-auto overflow-y-visible">
        <table class="w-full text-left text-sm text-body">
            {% include '_partials/table/_header.html.twig' with {
                route: 'app_admin_equipments_index',
                params: params,
                columns: columns
            } %}
            <tbody class="divide-y divide-neutral-secondary-soft bg-neutral-primary">
                {% for equipment in pager %}
                    <tr class="border-b border-default bg-neutral-primary-soft hover:bg-neutral-secondary-medium cursor-pointer" data-row-link="{{ path('app_admin_equipments_show', { id: equipment.id }) }}">
                        <th scope="row" class="px-6 py-4 font-semibold text-heading whitespace-nowrap">{{ equipment.label }}</th>
                        <td class="px-6 py-4">{{ equipment.equipmentType.label }}</td>
                        <td class="px-6 py-4 text-center">
                            {{ include('components/_actions_dropdown.html.twig', {
                                id: 'equipment-actions-' ~ equipment.id,
                                items: [
                                    { label: 'Voir', href: path('app_admin_equipments_show', { id: equipment.id }) },
                                    { label: 'Éditer', href: path('app_admin_equipments_edit', { id: equipment.id }) },
                                    {
                                        label: 'Supprimer',
                                        href: '#',
                                        extra_class: 'text-fg-danger font-medium',
                                        data_attrs: {
                                            'data-modal-target': 'equipment-delete-modal',
                                            'data-modal-toggle': 'equipment-delete-modal',
                                            'data-delete-url': path('app_admin_equipments_delete', { id: equipment.id }),
                                            'data-delete-name': equipment.label
                                        }
                                    }
                                ]
                            }) }}
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td class="px-6 py-6 text-center text-sm text-body-subtle" colspan="{{ columns|length }}">Aucun équipement ne correspond à votre recherche.</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>

{% include '_partials/table/_pagination.html.twig' with { route: 'app_admin_equipments_index', params: params, pager: pager } %}
