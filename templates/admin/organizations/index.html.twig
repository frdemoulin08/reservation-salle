{% extends 'admin/_layout.html.twig' %}

{% block title %}Organisations{% endblock %}

{% set active_menu = 'organisations' %}
{% set page_heading = 'Organisations' %}

{% block admin_breadcrumb %}
    <div class="mb-4">
        {% include 'admin/organizations/_breadcrumb.html.twig' with { current_label: page_heading } %}
    </div>
{% endblock %}

{% block admin_content %}
    <div class="mb-8 flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between">
        <div>
            <p class="text-xs font-semibold uppercase tracking-[0.18em] text-brand">Gestion</p>
            <h1 class="mt-3 text-3xl font-semibold text-heading">{{ page_heading }}</h1>
            <p class="mt-2 max-w-2xl text-sm text-body">Gestion des organisations (structures juridiques). Les usagers peuvent aussi mettre à jour ces informations depuis leur espace personnel.</p>
        </div>
    </div>

    <section class="rounded-2xl border border-default bg-neutral-primary-soft p-5 shadow-xs">
        {{ include('components/_table_header.html.twig', {
            route: 'app_admin_organizations_index',
            params: params,
            target: 'organizations',
            search_id: 'organizations-search',
            search_placeholder: 'Rechercher une organisation, un SIRET ou un nom',
            search_value: params.filters.q ?? '',
            actions: [
                {
                    label: 'Ajouter une organisation',
                    variant: 'primary',
                    href: path('app_admin_organizations_new'),
                    size: 'sm',
                    extra_class: 'flex items-center justify-center',
                    icon: '<svg class="mr-2 h-3.5 w-3.5" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true"><path fill-rule="evenodd" clip-rule="evenodd" d="M10 3a1 1 0 011 1v5h5a1 1 0 110 2h-5v5a1 1 0 11-2 0v-5H4a1 1 0 110-2h5V4a1 1 0 011-1z" /></svg>'
                }
            ]
        }) }}
        <div id="organizations-table-container" data-table-ajax="organizations">
            {% include 'admin/organizations/_table.html.twig' with { pager: pager, params: params } %}
        </div>
    </section>

    {% include 'admin/_partials/_delete_modal.html.twig' with {
        modal_id: 'organization-delete-modal',
        message: 'Confirmez la suppression de cette organisation ? ',
        csrf_token_id: 'delete_organization',
        show_name: true
    } %}
{% endblock %}
